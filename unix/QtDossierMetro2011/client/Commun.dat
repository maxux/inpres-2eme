

typedef struct
   { int        N;              // Numero de la station
     int        L;              // Ligne
     int        C;              // Colonne
   } POSITION;


#ifdef CHEMIN
#include <qcolor.h>
typedef struct
   { POSITION   Position[15];
     QColor        Couleur;
   } LIGNEMETRO;
#endif

typedef struct
   { int        Visite;
     int        Suivant[6];
   } NOEUD;
